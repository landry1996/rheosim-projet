# =============================================================================
# APPLICATION SECRETS TEMPLATE
# =============================================================================
# Copy this file to application-secret.yml and fill in real values.
# NEVER commit application-secret.yml to version control.
# =============================================================================

spring:
  datasource:
    username: YOUR_DB_USERNAME
    password: YOUR_DB_PASSWORD

rheosim:
  security:
    jwt:
      secret-key: YOUR_JWT_SECRET_KEY_MIN_256_BITS_BASE64_ENCODED
      # Generate with: openssl rand -base64 64
