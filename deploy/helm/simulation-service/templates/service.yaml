apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-simulation-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: simulation-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    app: simulation-service
